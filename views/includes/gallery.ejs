<% if(itemList !== null){ %>
    <div class="gallery">
        <ul>
            <% for(i = 0; i < itemList.length; i++){ %>
            <li class="galleryItem">
                <img class="thumbnail" src= <%= itemList[i].imageFile %>>
                <h2><%= itemList[i].name %> </h2>
                <p class="itemUser"><%= itemList[i].userId %> </p>
                <p class="itemTags">Tags: 
                <% for(j = 0; j < itemList[i].tags.length; j++) { %> 
                <%=itemList[i].tags[j]%>
                <% if((j + 1) != itemList[i].tags.length) { %>,
                <% }} %> 
                </p>
            </li> 
            <% } %>
        </ul>
    </div>
<% } else { %>
    <h1>No art found!</h1>
<% } %>